<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools" xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_import"
        android:title="Import All"
        app:showAsAction="never|withText" />
    <item
        android:id="@+id/action_export"
        android:title="Export All"
        app:showAsAction="never|withText" />
    <item
        android:id="@+id/action_copy"
        android:title="Copy to Instance"
        app:showAsAction="never|withText" />
    <item
        android:id="@+id/action_resetall"
        android:title="Reset Instance"
        app:showAsAction="never|withText" />
</menu>